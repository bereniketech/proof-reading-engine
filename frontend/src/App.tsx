function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Proof-Reading Engine</p>
        <h1>Frontend scaffold is ready</h1>
        <p>Task T-01 initialized this Vite + React + TypeScript workspace.</p>
      </section>
    </main>
  );
}

export default App;
